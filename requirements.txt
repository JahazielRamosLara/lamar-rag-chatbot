# ---- Backend / API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# ---- Base de datos (AWS RDS PostgreSQL + pgvector) ----
asyncpg==0.30.0          # driver async usado por el backend
psycopg[binary]==3.2.3   # driver sync usado por los scripts de ingesta
pgvector==0.3.6

# ---- AWS Bedrock (embeddings Titan + LLM Claude) ----
boto3==1.35.90
anthropic[bedrock]==0.42.0

# ---- Procesamiento del PDF del reglamento ----
pdfplumber==0.11.4
pypdf==5.1.0
